#!/usr/bin/with-contenv bash
# Create data subdirectories + wine prefix with correct ownership
set -e

mkdir -p /data/{config,downloads,experts,indicators,scripts,signals,logs,reports,wine}
chown -R abc:abc /data
